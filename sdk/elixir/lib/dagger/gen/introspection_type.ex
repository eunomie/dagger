# This file generated by `dagger_codegen`. Please DO NOT EDIT.
defmodule Dagger.IntrospectionType do
  @moduledoc """
  A type defined in a GraphQL introspection schema.
  """

  use Dagger.Core.Base, kind: :object, name: "IntrospectionType"

  alias Dagger.Core.Client
  alias Dagger.Core.QueryBuilder, as: QB

  @derive Dagger.ID

  defstruct [:query_builder, :client]

  @type t() :: %__MODULE__{}

  @doc """
  The description of the type.
  """
  @spec description(t()) :: {:ok, String.t()} | {:error, term()}
  def description(%__MODULE__{} = introspection_type) do
    query_builder =
      introspection_type.query_builder |> QB.select("description")

    Client.execute(introspection_type.client, query_builder)
  end

  @doc """
  The directives applied to the type, including @sourceModuleName for module-defined types.
  """
  @spec directives(t()) :: {:ok, [Dagger.IntrospectionDirective.t()]} | {:error, term()}
  def directives(%__MODULE__{} = introspection_type) do
    query_builder =
      introspection_type.query_builder |> QB.select("directives") |> QB.select("id")

    with {:ok, items} <- Client.execute(introspection_type.client, query_builder) do
      {:ok,
       for %{"id" => id} <- items do
         %Dagger.IntrospectionDirective{
           query_builder:
             QB.query()
             |> QB.select("loadIntrospectionDirectiveFromID")
             |> QB.put_arg("id", id),
           client: introspection_type.client
         }
       end}
    end
  end

  @doc """
  The possible values of the type. Null unless the type is an enum.
  """
  @spec enum_values(t()) :: {:ok, [Dagger.IntrospectionEnumValue.t()]} | {:error, term()}
  def enum_values(%__MODULE__{} = introspection_type) do
    query_builder =
      introspection_type.query_builder |> QB.select("enumValues") |> QB.select("id")

    with {:ok, items} <- Client.execute(introspection_type.client, query_builder) do
      {:ok,
       for %{"id" => id} <- items do
         %Dagger.IntrospectionEnumValue{
           query_builder:
             QB.query()
             |> QB.select("loadIntrospectionEnumValueFromID")
             |> QB.put_arg("id", id),
           client: introspection_type.client
         }
       end}
    end
  end

  @doc """
  The fields of the type. Null unless the type is an object or interface.
  """
  @spec fields(t()) :: {:ok, [Dagger.IntrospectionField.t()]} | {:error, term()}
  def fields(%__MODULE__{} = introspection_type) do
    query_builder =
      introspection_type.query_builder |> QB.select("fields") |> QB.select("id")

    with {:ok, items} <- Client.execute(introspection_type.client, query_builder) do
      {:ok,
       for %{"id" => id} <- items do
         %Dagger.IntrospectionField{
           query_builder:
             QB.query()
             |> QB.select("loadIntrospectionFieldFromID")
             |> QB.put_arg("id", id),
           client: introspection_type.client
         }
       end}
    end
  end

  @doc """
  A unique identifier for this IntrospectionType.
  """
  @spec id(t()) :: {:ok, Dagger.IntrospectionTypeID.t()} | {:error, term()}
  def id(%__MODULE__{} = introspection_type) do
    query_builder =
      introspection_type.query_builder |> QB.select("id")

    Client.execute(introspection_type.client, query_builder)
  end

  @doc """
  The input fields of the type. Null unless the type is an input object.
  """
  @spec input_fields(t()) :: {:ok, [Dagger.IntrospectionInputValue.t()]} | {:error, term()}
  def input_fields(%__MODULE__{} = introspection_type) do
    query_builder =
      introspection_type.query_builder |> QB.select("inputFields") |> QB.select("id")

    with {:ok, items} <- Client.execute(introspection_type.client, query_builder) do
      {:ok,
       for %{"id" => id} <- items do
         %Dagger.IntrospectionInputValue{
           query_builder:
             QB.query()
             |> QB.select("loadIntrospectionInputValueFromID")
             |> QB.put_arg("id", id),
           client: introspection_type.client
         }
       end}
    end
  end

  @doc """
  The interfaces implemented by the type. Null unless the type is an object or interface.
  """
  @spec interfaces(t()) :: {:ok, [Dagger.IntrospectionType.t()]} | {:error, term()}
  def interfaces(%__MODULE__{} = introspection_type) do
    query_builder =
      introspection_type.query_builder |> QB.select("interfaces") |> QB.select("id")

    with {:ok, items} <- Client.execute(introspection_type.client, query_builder) do
      {:ok,
       for %{"id" => id} <- items do
         %Dagger.IntrospectionType{
           query_builder:
             QB.query()
             |> QB.select("loadIntrospectionTypeFromID")
             |> QB.put_arg("id", id),
           client: introspection_type.client
         }
       end}
    end
  end

  @doc """
  The kind of the type, e.g. "OBJECT", "INTERFACE", "ENUM" or "SCALAR".
  """
  @spec kind(t()) :: {:ok, String.t()} | {:error, term()}
  def kind(%__MODULE__{} = introspection_type) do
    query_builder =
      introspection_type.query_builder |> QB.select("kind")

    Client.execute(introspection_type.client, query_builder)
  end

  @doc """
  The name of the type.
  """
  @spec name(t()) :: {:ok, String.t()} | {:error, term()}
  def name(%__MODULE__{} = introspection_type) do
    query_builder =
      introspection_type.query_builder |> QB.select("name")

    Client.execute(introspection_type.client, query_builder)
  end
end

defimpl Jason.Encoder, for: Dagger.IntrospectionType do
  def encode(introspection_type, opts) do
    {:ok, id} = Dagger.IntrospectionType.id(introspection_type)
    Jason.Encode.string(id, opts)
  end
end

defimpl Nestru.Decoder, for: Dagger.IntrospectionType do
  def decode_fields_hint(_struct, _context, id) do
    {:ok, Dagger.Client.load_introspection_type_from_id(Dagger.Global.dag(), id)}
  end
end
